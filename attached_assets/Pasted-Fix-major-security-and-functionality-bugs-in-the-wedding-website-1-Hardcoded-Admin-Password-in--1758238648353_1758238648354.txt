Fix major security and functionality bugs in the wedding website:

1. **Hardcoded Admin Password in api/admin/login/route.ts**:
   - Remove the hardcoded 'STAYIRIE' check (password !== 'STAYIRIE').
   - Use lib/crypto.ts hashPassword/verifyPassword to hash and verify passwords.
   - Update admin_users table in Supabase to store password_hash and salt for BRIDE, GROOM, PLANNER.
   - Set individual passwords: Bride: BRIDESTAYIRIE, Groom: GROOMSTAYIRIE, Planner: PLANNERSTAYIRIE.
   - Ensure api/admin/login/route.ts queries the table for authentication.

2. **Environment Variable Misconfiguration in lib/subabase.ts**:
   - Correct swapped env vars: Use NEXT_PUBLIC_SUPABASE_URL as supabaseUrl, NEXT_PUBLIC_SUPABASE_ANON_KEY as supabaseAnonKey.
   - Rename file to supabase.ts (fix typo).
   - Verify .env.local has: NEXT_PUBLIC_SUPABASE_URL=https://miebpemmwyedqluiqall.supabase.co and NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1pZWJwZW1td3llZHFsdWlxYWxsIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTc5OTA5MzcsImV4cCI6MjA3MzU2NjkzN30.lf_eYwMxDdoek3OWVkju3TucVwCZ7FxJT0HzisJ7vG0.

3. **Missing RSVP Table in api/rsvps/route.ts**:
   - Ensure rsvps table exists in Supabase with columns: id, guest_name, email, attendance, guest_count, dietary_restrictions, special_message, wallet_address, created_at.
   - Remove sample data fallback in api/rsvps/route.ts—rely on real Supabase data.
   - Test API returns actual RSVPs.

4. **Insecure Cookie Handling in api/admin/login/route.ts**:
   - Force secure: true for admin-token cookie (remove NODE_ENV condition) since Replit uses HTTPS proxy.
   - Update: response.cookies.set('admin-token', token, { httpOnly: true, secure: true, sameSite: 'strict', maxAge: 24 * 60 * 60 }).

5. **Unimplemented api/rsvp/route.ts**:
   - Create app/api/rsvp/route.ts to handle RSVP form POST from rsvp/page.tsx.
   - Implement: Insert data into rsvps table via supabase.from('rsvps').insert(rsvpData).select(), return { success: true, data } on success, 500 on error.

Test all changes:
- Admin login with new passwords: Bride: BRIDESTAYIRIE, Groom: GROOMSTAYIRIE, Planner: PLANNERSTAYIRIE.
- RSVP submission updates rsvps table.
- Seating API and admin/rsvps API return real data.
Restart server after fixes. Log any errors.
